<!doctype html>
<html lang="en">
  <head>
    <title>Fixtures</title>

    <%- include('partials/tags.ejs') %>
  </head>
  <body>
    <%- include('partials/navbar.ejs') %>

    <!-- Heading -->
    <section>
      <div class="container">
        <div class="row">
          <div class="col text-center">
            <% if(locals.sport) %>
            <h3 class="text-capitalize"><%= locals.sport %> Fixtures</h3>
          </div>
        </div>
      </div>
    </section>
    <!-- Heading -->

    <div class="pb-2"></div>

    <% if(locals.sport == 'football') { %>
      <%- include('partials/fixtures/fixtures-football.ejs') %>
    <% } %>
    <% if(locals.sport == 'cricket') { %>
      <%- include('partials/fixtures/fixtures-cricket.ejs') %>
    <% } %>

    <%- include('partials/footer.ejs') %>

    <%- include('partials/scripts.ejs') %>
  </body>
</html>